You are working in `/mnt/SHIT/Projects/Firmius`.

Task: implement a focused retry/resume + raw provider error passthrough hunk.

Why this hunk is needed
Two current behaviors are wrong for real dogfooding:

1. Retry semantics are overcomplicated and still not what the user wants.
The user does NOT want “retry last retryable request” or “replay last user message”.
The desired behavior is much simpler:

- if the focused agent is stopped in `Error` or `Cancelled`
- the user should be able to retry immediately
- this should resume that same agent from its current context/state
- it should NOT append a new user message
- it should effectively “continue from here”

2. Provider errors are still too filtered/abstracted.
The user wants the actual raw provider error JSON/body surfaced on every failed request for every provider, so it is obvious what the provider actually returned.

Goal
1. Replace retry-request semantics with retry/resume-current-agent semantics.
2. Pass through raw provider error payloads for all providers on failed requests.
3. Keep the full suite green.

Important
Do NOT keep building on the old retryable-request model.
If it is no longer needed, simplify or remove it.
The new mental model should be:
- retry == resume stopped agent from current context

Required changes

1. Retry/resume semantics
Implement retry so that:
- if the focused agent is in `Error` or `Cancelled`
- the user can retry it
- no new user turn/message is appended
- the agent resumes from existing context/history

Desired behavior:
- interrupted lead -> retry resumes that lead
- provider/runtime error -> retry resumes that same agent
- reload thread -> retry still works if the focused agent can be resumed from persisted history

This is not “resend the last message”.
This is “resume the stopped agent”.

2. Remove or sharply de-emphasize the old retryable-request logic
The current request-replay model is too complex and solves the wrong problem.

Use judgment:
- remove it if feasible
- or leave only minimal fallback compatibility if truly necessary

But the primary and intended retry path must become:
- resume current stopped agent

3. Retry availability
Retry should be available when:
- focused agent status is `Error`
- or focused agent status is `Cancelled`

If the focused agent is not stopped in one of those states:
- no retry action
- show a clear status message if needed

4. Persist/resume across reload
After reloading a thread:
- if the focused agent was previously in `Error` or `Cancelled`
- retry should still resume that agent from persisted history/context
- no synthetic new user message

This likely requires:
- making sure persisted agent state/status is available enough
- ensuring the resume path can re-enter the run loop cleanly

5. Engine/harness support for resume-without-new-user-message
Add a clean path for:
- running an agent again
- without appending a new `user-task-*` turn

Do NOT fake this by sending an empty user message.
Do NOT append placeholder text.
Resume from current context directly.

6. Raw provider error passthrough for all providers
On failed provider requests, preserve and surface the raw provider error payload/body whenever available.

This applies to all providers, not just Qwen.

Desired behavior:
- if provider returns structured JSON error body, include it in surfaced error details
- if provider returns plain text error body, include that
- preserve HTTP status
- preserve provider/account/model context where available

Do not replace friendly summaries entirely if they are useful.
But the raw body must be available in the final surfaced error.

Examples:
- provider 429 with JSON body -> UI/journal should show the actual JSON/body
- provider 400 validation error -> actual returned body should be visible
- provider auth error -> actual returned body should be visible

7. Do not regress current retry classification fixes
Keep:
- terminal invalid 400 request-shape errors are not retried automatically
- no pointless provider retry loops
- no fake switching to the same single account

Likely files
- `packages/core/src/Harness.cpp`
- `packages/core/include/harness/Harness.hpp`
- `packages/core/src/Engine.cpp`
- maybe `packages/core/include/Engine.hpp`
- `packages/core/src/agents/Agent.cpp`
- provider implementations:
  - `packages/provider/src/providers/QwenProvider.cpp`
  - and any shared provider base layers used by others
- related TUI hotkey/help/state files if retry UI needs adjustment
- tests covering retry/resume behavior and provider error passthrough

Implementation guidance
1. Keep the retry model simple: resume stopped agent.
2. Avoid replaying messages unless absolutely necessary as fallback.
3. Preserve exact provider error bodies.
4. Keep UX clear and debuggable.
5. Keep the full suite green.

Testing requirements
Add/extend tests for at least:
- retry resumes a cancelled agent without appending a new user turn
- retry resumes an errored agent without appending a new user turn
- retry works after thread reload/resume
- retry is unavailable when focused agent is not stopped
- provider error bodies are surfaced/preserved in failure paths for at least:
  - one shared/base provider path
  - Qwen path

Required verification
- Build all targets with:
  - `cmake --build build -j16`
- Run:
  - `ctest --test-dir build --output-on-failure`
- If focused tests or audits help, run them and report clearly
- Final result must keep full ctest green

Deliverable
- Make the code changes directly.
- At the end, summarize:
  - files changed
  - new retry/resume semantics
  - what old retry-request logic remains or was removed
  - raw provider error passthrough changes
  - tests added/updated
  - final ctest result
  - any remaining caveats
