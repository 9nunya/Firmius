You are working in `/mnt/SHIT/Projects/Firmius`.

Task: implement a focused retry-last-turn + Qwen 429/raw-error hunk.

Why this hunk is needed
Recent dogfooding and review show the previous retry/resume hunk is still not aligned with the actual desired UX, and provider error passthrough is still incomplete.

Current problems:

1. Retry semantics are still wrong.
The user does NOT want retry to depend on the currently focused agent's live/persisted stopped status.
The user wants:
- retry the LAST TURN SENT
- if that turn ended in `Error` or `Cancelled`
- resume from that point
- no new user message appended

The current implementation still effectively gates retry on the focused agent state, which is too brittle.

2. Qwen provider error passthrough is still incomplete.
In single-account paths, failures still collapse into messages like:
- `No alternate Qwen account available after failure.`
- `All qualified accounts exhausted.`

That hides the actual provider body/cause.
The user wants the actual provider error JSON/body passed through on EVERY failed request, for ALL providers.

3. The newest dogfood run showed a real Qwen 429 path.
Live audit on thread `9c785190-2141-4c15-9aa3-f0e0604e6433` / agent `7ecb50d4-1aff-452b-9ba7-cbba01907d5e` showed:
- HTTP 429
- `Quota exhausted or rate limited. Switching to next account...`
- then no alternate account

So the next fix must preserve that real provider body in the surfaced error path, not just the abstracted summary.

Goal
1. Make retry mean "resume the last failed/interrupted turn", not "resume current focused stopped agent if its status lines up".
2. Preserve raw provider error payloads all the way through final surfaced errors, including no-alternate-account Qwen paths.
3. Keep full ctest green.

Important
Do NOT reintroduce request replay semantics.
Do NOT append a new user turn.
Do NOT hide the raw provider body behind a synthesized summary.

Required changes

1. Retry must be based on the last turn, not the current agent status gate
Implement retry so that it targets the last turn sent for the relevant agent/thread context.

Desired behavior:
- if the most recent execution ended in `Error` or `Cancelled`, retry should resume from there
- this should work even if the current focused agent state/status is awkward after reload
- retry should not depend on fragile live-state gating as the primary decision

Use the persisted history/turn model as the source of truth.

The intended contract is:
- retry the last failed/interrupted turn
- resume from that context
- no new user message

2. Define a clear retry target
Add a clean notion of the "last resumable turn" or equivalent.

It should:
- survive reload
- be derived from persisted history/state, not transient UI state
- support both `Error` and `Cancelled`
- preserve images if that turn included them

Use judgment on whether this is:
- explicit metadata
- or robust reconstruction from history

But it must be centered on the actual last turn, not just "focused agent is stopped right now".

3. Retry availability
Retry should be available when the last relevant turn ended in:
- `Error`
- `Cancelled`

Retry should not be blocked just because:
- a focused agent object was recreated
- current in-memory state is slightly different
- the UI focus path is brittle

If nothing resumable exists, fail clearly.

4. No new user turn on retry
Keep this invariant:
- retry/resume must not append a new `user-task-*` turn
- it must continue from existing history/context

Add/keep regression coverage for that.

5. Raw provider error passthrough for all providers must be complete
The final surfaced error path must preserve raw provider bodies whenever available.

This includes:
- shared OpenAI-style provider path
- Qwen path
- final no-alternate-account / exhausted-account paths

If a provider returned a JSON error body, that JSON/body must still be visible in the surfaced error details.
If a provider returned plain text, that must still be visible.

Friendly summaries are fine, but they must not replace the raw body.

6. Qwen no-alternate-account path must preserve the real underlying body
Fix the specific Qwen path where the final error currently collapses to:
- `No alternate Qwen account available after failure.`

Desired behavior:
- include the real underlying provider failure body/cause
- then append the account-availability context if useful

Examples of acceptable final messaging:
- `<raw 429 body ...> No alternate Qwen account available after failure.`
- `<raw 400 body ...> No alternate Qwen account available after failure.`

But do not lose the raw body.

7. Investigate local quota-state interaction enough to avoid misleading output
You do NOT need to solve all quota refresh design in this hunk unless required.
But if local cached quota/exhausted state is contributing to misleading "all qualified accounts exhausted" behavior, tighten the messaging or logic enough that the surfaced error is truthful and debuggable.

Do not overbuild.

Likely files
- `packages/core/src/Harness.cpp`
- `packages/core/include/harness/Harness.hpp`
- maybe `packages/shared/include/Context.hpp`
- maybe `packages/shared/src/Serialization.cpp`
- `packages/core/src/Engine.cpp`
- `packages/core/src/agents/Agent.cpp`
- `packages/provider/src/providers/QwenProvider.cpp`
- maybe `packages/provider/src/providers/BaseOpenAIProvider.cpp`
- related tests under `tests/unit/harness/` and `tests/unit/provider/`

Implementation guidance
1. Prefer a simple mental model: retry the last failed/interrupted turn.
2. Use persisted history as the primary source of truth.
3. Keep image preservation intact.
4. Preserve raw provider bodies in surfaced failures.
5. Do not regress the earlier non-retryable 400 fix.

Testing requirements
Add/extend tests for at least:
- retry uses the last failed/interrupted turn, not just live focused-agent status
- retry still works after reload for an errored turn
- retry still works after reload for a cancelled turn
- retry does not append a new `user-task-*` turn
- retry preserves images
- Qwen final no-alternate-account error still contains the raw underlying provider body
- shared provider error formatting still preserves raw body

Required verification
- Build all targets with:
  - `cmake --build build -j16`
- Run:
  - `ctest --test-dir build --output-on-failure`
- If focused tests or audits help, run them and report clearly
- Final result must keep full ctest green

Deliverable
- Make the code changes directly.
- At the end, summarize:
  - files changed
  - new retry-last-turn behavior
  - retry availability logic
  - Qwen/raw-error passthrough fixes
  - tests added/updated
  - final ctest result
  - any remaining caveats
